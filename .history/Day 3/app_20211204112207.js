var fs = require("fs");
var text = fs.readFileSync("./Day 2/input.txt").toString('utf-8');

let grid = [[]];

for (let i=0; i<text.length; i++){
    switch (direction) {
        case '>':

        case '^':

        case '<':

        case 'v':
    }
}